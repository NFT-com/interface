query ProfileFollowers($input: FollowersInput!) {
  profileFollowers(input: $input) {
    pageInfo {
      firstCursor
      lastCursor
    }
    items {
      address
      chainId
      network
    }
  }
}
