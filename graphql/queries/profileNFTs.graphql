query ProfileNFTs($input: NFTsInput!) {
  nfts(input: $input) {
    pageInfo {
      firstCursor
      lastCursor
    }
    items {
      size
      nft {
        contract
        id
        tokenId
        metadata {
          imageURL
          description
          name
        }
      }
    }
  }
}
