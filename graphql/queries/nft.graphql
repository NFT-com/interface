query Nft($contract: Address!, $id: String!) {
  nft(contract: $contract, id: $id) {
    id
    isOwnedByMe
    price
    contract
    tokenId
    type
    wallet {
      address
      chainId
      preferredProfile {
        url
        photoURL
      }
    }
    metadata {
      name
      imageURL
      description
      traits {
        type
        value
      }
    }
  }
}