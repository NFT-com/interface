query Nft($contract: Address!, $id: String!) {
  nft(contract: $contract, id: $id) {
    id
    isOwnedByMe
    price
    contract
    tokenId
    type
    wallet {
      address
      chainId
    }
    metadata {
      name
      imageURL
      description
      traits {
        type
        value
      }
    }
  }
}