query RecentProfiles($input: LatestProfilesInput) {
  latestProfiles(input: $input) {
    items {
      id
      bannerURL
      isGKMinted
      owner {
        address
      }
      photoURL
      url
      visibleNFTs
    }
    pageInfo {
      firstCursor
      lastCursor
    }
    totalItems
  }
}
