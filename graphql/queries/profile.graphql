query Profile($url: String!) {
  profile(url: $url) {
    id
    url
    status
    bannerURL
    photoURL
    description
    gkIconVisible
    displayType
    owner {
      address
      chainId
      network
    }
  }
}
