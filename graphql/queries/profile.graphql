query Profile($url: String!) {
  profile(url: $url) {
    id
    url
    status
    bannerURL
    photoURL
    description
    isFollowedByMe
    gkIconVisible
    owner {
      address
      chainId
      network
    }
  }
}
