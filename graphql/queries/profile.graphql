query Profile($url: String!) {
  profile(url: $url) {
    id
    url
    status
    bannerURL
    photoURL
    description
    gkIconVisible
    nftsDescriptionsVisible
    displayType
    owner {
      address
      chainId
      network
    }
  }
}
