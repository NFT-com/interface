query Profile($url: String!, $chainId: String!) {
  profile(url: $url, chainId: $chainId) {
    id
    url
    status
    bannerURL
    photoURL
    description
    gkIconVisible
    nftsDescriptionsVisible
    layoutType
    owner {
      address
      chainId
      network
    }
    profileView
  }
}
