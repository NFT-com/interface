query Profile($url: String!) {
  profile(url: $url) {
    id
    url
    status
    bannerURL
    photoURL
    description
    isFollowedByMe
    owner {
      address
      chainId
      network
    }
  }
}
