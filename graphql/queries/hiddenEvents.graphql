query IgnoredEvents($input: IgnoredEventsInput!) {
  ignoredEvents(input: $input) {
    id
    chainId
    contract
    eventName
    txHash
    ownerAddress
    profileUrl
    destinationAddress
    blockNumber
    ignore
    hideIgnored
  }
}