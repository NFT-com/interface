query OfficialCollections($input: OfficialCollectionsInput!) {
  officialCollections(input: $input) {
    items {
      id
      contract
      slug
      chainId
      updatedAt
    }
    pageCount
    totalItems
  }
}
