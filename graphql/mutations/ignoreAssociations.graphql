mutation IgnoreAssocations($eventIdArray: [String]!) {
  ignoreAssociations(eventIdArray: $eventIdArray) {
    id
    chainId
    contract
    eventName
    txHash
    ownerAddress
    profileUrl
    destinationAddress
    blockNumber
    ignore
  }
}